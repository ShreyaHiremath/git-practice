name: First GitHub Action

on:
  push:
    branches:
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Shreya! Your first GitHub Action is running."
